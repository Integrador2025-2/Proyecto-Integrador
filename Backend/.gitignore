# Ignorar artefactos de compilación
/bin/
/obj/

# Visual Studio y archivos de usuario
.vs/
*.user
*.suo
*.userosscache
*.sln.cache

# Archivos de configuración que pueden contener cadenas de conexión / secretos
# NOTE: appsettings are local config / may contain secrets; keep ignored by default
appsettings.json
appsettings.*.json
secrets.json

# Environment files - ignore all except examples
.env
.env*
.env.*
**/.env
**/.env.*
!.env.example
!.env.sample
!.env.template


# Archivos binarios y temporales
*.dll
*.exe
*.pdb

# Node/npm artefactos (por si hay frontends)
node_modules/

# Logs
*.log

# Otros
*.cache

# Fin

# Ensure EF Migrations are tracked (do NOT ignore the migrations C# files)
!Migrations/
!Migrations/*.cs

# Scripts and docs we want in the repo
!scripts/
!docs/
